#!/usr/bin/env bash
#
# gastown is brew-managed via ./Brewfile.
# This stub exists so script/install (which auto-discovers install.sh at
# depth 2) doesn't fail on `gastown` as a topic with no installer.

exit 0
