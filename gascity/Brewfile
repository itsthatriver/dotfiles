# gascity is built from source — see install.sh
# tap "gastownhall/gascity"
# brew "gastownhall/gascity/gascity"

# flock for launchd job locking
brew "flock"

# dolt — beads data plane (shared with gastown)
brew "dolt"
