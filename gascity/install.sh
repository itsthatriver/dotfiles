#!/usr/bin/env bash
#
# DISABLED 2026-04-28 — gascity is now brew-managed; see ./Brewfile.
# Preserved as a no-op so script/install (which auto-discovers install.sh
# at depth 2) doesn't fail. The previous source-build logic lives in git
# history if it needs to come back.

exit 0
